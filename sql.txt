-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.ai_conversations (
  id bigint NOT NULL DEFAULT nextval('ai_conversations_id_seq'::regclass),
  student_id bigint NOT NULL,
  latest_assessment_id bigint,
  title character varying NOT NULL DEFAULT 'Chat với AI'::character varying,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  last_message_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_conversations_pkey PRIMARY KEY (id),
  CONSTRAINT ai_conversations_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT ai_conversations_latest_assessment_id_fkey FOREIGN KEY (latest_assessment_id) REFERENCES public.assessments(id)
);
CREATE TABLE public.ai_messages (
  id bigint NOT NULL DEFAULT nextval('ai_messages_id_seq'::regclass),
  conversation_id bigint NOT NULL,
  role character varying NOT NULL CHECK (role::text = ANY (ARRAY['user'::character varying, 'assistant'::character varying]::text[])),
  content text NOT NULL,
  related_assessment_id bigint,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT ai_messages_pkey PRIMARY KEY (id),
  CONSTRAINT ai_messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.ai_conversations(id),
  CONSTRAINT ai_messages_related_assessment_id_fkey FOREIGN KEY (related_assessment_id) REFERENCES public.assessments(id)
);
CREATE TABLE public.alembic_version (
  version_num character varying NOT NULL,
  CONSTRAINT alembic_version_pkey PRIMARY KEY (version_num)
);
CREATE TABLE public.assessments (
  id integer NOT NULL DEFAULT nextval('assessments_id_seq'::regclass),
  student_id integer NOT NULL,
  answers json NOT NULL,
  total_score integer NOT NULL,
  severity_level character varying NOT NULL,
  analysis text,
  recommendations json,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  functional_impairment integer,
  CONSTRAINT assessments_pkey PRIMARY KEY (id),
  CONSTRAINT assessments_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.conversations (
  id integer NOT NULL DEFAULT nextval('conversations_id_seq'::regclass),
  student_id integer NOT NULL,
  title character varying,
  is_active boolean,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone,
  last_message_at timestamp with time zone,
  CONSTRAINT conversations_pkey PRIMARY KEY (id),
  CONSTRAINT conversations_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id)
);
CREATE TABLE public.counselor_conversations (
  id bigint NOT NULL DEFAULT nextval('counselor_conversations_id_seq'::regclass),
  student_id integer NOT NULL,
  counselor_id integer NOT NULL,
  status character varying NOT NULL DEFAULT 'active'::character varying CHECK (status::text = ANY (ARRAY['active'::character varying, 'closed'::character varying, 'archived'::character varying]::text[])),
  last_message_at timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT counselor_conversations_pkey PRIMARY KEY (id),
  CONSTRAINT fk_counselor_conversations_student FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT fk_counselor_conversations_counselor FOREIGN KEY (counselor_id) REFERENCES public.counselors(id)
);
CREATE TABLE public.counselor_messages (
  id bigint NOT NULL DEFAULT nextval('counselor_messages_id_seq'::regclass),
  conversation_id bigint NOT NULL,
  sender_type character varying NOT NULL CHECK (sender_type::text = ANY (ARRAY['student'::character varying, 'counselor'::character varying]::text[])),
  content text NOT NULL,
  is_read boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT counselor_messages_pkey PRIMARY KEY (id),
  CONSTRAINT fk_counselor_messages_conversation FOREIGN KEY (conversation_id) REFERENCES public.counselor_conversations(id)
);
CREATE TABLE public.counselors (
  id integer NOT NULL DEFAULT nextval('counselors_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  license_number character varying UNIQUE,
  specialization character varying,
  years_of_experience integer,
  bio text,
  phone_number character varying,
  office_location character varying,
  is_available boolean,
  CONSTRAINT counselors_pkey PRIMARY KEY (id),
  CONSTRAINT counselors_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.medical_centers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name character varying NOT NULL,
  address text NOT NULL,
  latitude numeric NOT NULL,
  longitude numeric NOT NULL,
  phone character varying,
  email character varying,
  website character varying,
  services ARRAY DEFAULT ARRAY[]::text[],
  opening_hours jsonb DEFAULT '{}'::jsonb,
  description text,
  image_url text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT medical_centers_pkey PRIMARY KEY (id)
);
CREATE TABLE public.messages (
  id integer NOT NULL DEFAULT nextval('messages_id_seq'::regclass),
  conversation_id integer NOT NULL,
  role character varying NOT NULL,
  content text NOT NULL,
  voice_analysis_id integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT messages_pkey PRIMARY KEY (id),
  CONSTRAINT messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.conversations(id),
  CONSTRAINT messages_voice_analysis_id_fkey FOREIGN KEY (voice_analysis_id) REFERENCES public.voice_analyses(id)
);
CREATE TABLE public.parent_consents (
  id integer NOT NULL DEFAULT nextval('parent_consents_id_seq'::regclass),
  student_id integer NOT NULL,
  parent_id integer NOT NULL,
  is_approved integer,
  CONSTRAINT parent_consents_pkey PRIMARY KEY (id),
  CONSTRAINT parent_consents_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT parent_consents_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.parents(id)
);
CREATE TABLE public.parents (
  id integer NOT NULL DEFAULT nextval('parents_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  phone_number character varying,
  address text,
  occupation character varying,
  CONSTRAINT parents_pkey PRIMARY KEY (id),
  CONSTRAINT parents_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.students (
  id integer NOT NULL DEFAULT nextval('students_id_seq'::regclass),
  user_id integer NOT NULL UNIQUE,
  student_code character varying,
  date_of_birth date,
  phone_number character varying,
  address text,
  university character varying,
  major character varying,
  gender character varying DEFAULT 'prefer_not_to_say'::character varying CHECK (gender::text = ANY (ARRAY['male'::character varying, 'female'::character varying, 'other'::character varying, 'prefer_not_to_say'::character varying]::text[])),
  emergency_contact_parent_id integer,
  education_level character varying CHECK (education_level::text = ANY (ARRAY['high_school'::character varying, 'undergraduate'::character varying, 'graduate'::character varying, 'other'::character varying]::text[])),
  grade character varying,
  CONSTRAINT students_pkey PRIMARY KEY (id),
  CONSTRAINT students_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT fk_students_emergency_contact_parent FOREIGN KEY (emergency_contact_parent_id) REFERENCES public.parents(id)
);
CREATE TABLE public.users (
  id integer NOT NULL DEFAULT nextval('users_id_seq'::regclass),
  email character varying NOT NULL,
  hashed_password character varying NOT NULL,
  full_name character varying NOT NULL,
  role USER-DEFINED NOT NULL,
  is_active boolean,
  is_verified boolean,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone,
  last_login timestamp with time zone,
  phone character varying,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.voice_analyses (
  id integer NOT NULL DEFAULT nextval('voice_analyses_id_seq'::regclass),
  student_id integer NOT NULL,
  audio_file_path character varying NOT NULL,
  audio_duration double precision,
  transcription text,
  transcription_language character varying,
  transcription_confidence double precision,
  detected_emotions json,
  dominant_emotion character varying,
  emotion_confidence double precision,
  processing_time double precision,
  processed_at timestamp with time zone DEFAULT now(),
  has_error integer,
  error_message text,
  assessment_id integer NOT NULL,
  file_size_bytes integer,
  audio_format character varying,
  prompt_id integer,
  prompt_text text,
  word_count integer,
  audio_features json,
  sentiment_score double precision,
  keywords json,
  psychological_markers json,
  gender_used character varying,
  normalized_features json,
  processing_status character varying NOT NULL DEFAULT 'pending'::character varying,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  comprehensive_analysis text,
  comprehensive_recommendations jsonb,
  CONSTRAINT voice_analyses_pkey PRIMARY KEY (id),
  CONSTRAINT voice_analyses_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.students(id),
  CONSTRAINT voice_analyses_assessment_id_fkey FOREIGN KEY (assessment_id) REFERENCES public.assessments(id)
);