# ============================================================
# Voice Service Dependencies - HYBRID APPROACH
# ============================================================
# Architecture:
#   1. Deepgram API → Transcription (external, lightweight)
#   2. Librosa → Audio feature extraction (local)
#   3. Custom ML → Emotion detection (local, our unique value!)
#
# Build size: ~150MB (vs 500MB with Whisper) ✅
# Memory usage: <512MB (fits Render free tier!) ✅
# ============================================================

# FastAPI framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Audio processing - FOR FEATURE EXTRACTION ONLY
# Note: We use Deepgram API for transcription (no Whisper!)
librosa==0.10.1              # Audio feature extraction (pitch, energy, etc.)
soundfile==0.12.1            # Audio I/O
audioread==3.0.1             # Audio file reading

# Scientific computing
numpy==1.24.3                # Numerical operations
scipy==1.11.4                # Signal processing (needed by librosa)

# File operations
aiofiles==23.2.1             # Async file handling

# Data validation
pydantic==2.5.0              # Data models
pydantic-settings==2.1.0     # Settings management

# Database (PostgreSQL)
psycopg2-binary==2.9.9       # PostgreSQL adapter
sqlalchemy==2.0.23           # ORM

# HTTP client
httpx==0.25.2                # For Deepgram API calls & ai-service communication

# Environment variables
python-dotenv==1.0.0         # Load .env files

# Vietnamese NLP (for text analysis)
underthesea==6.7.0           # Vietnamese tokenization & sentiment

# Utilities
python-dateutil==2.8.2       # Date/time utilities

# ============================================================
# REMOVED DEPENDENCIES (memory optimization):
# ============================================================
# ❌ openai-whisper==20231117  → Replaced by Deepgram API
#    Reason: 400MB+ build size, not deployable on free tier
#
# KEPT DEPENDENCIES (required for emotion detection):
# ============================================================
# ✅ librosa → Audio feature extraction (pitch, energy, rate)
# ✅ numpy/scipy → Signal processing for emotion detection
# ✅ underthesea → Vietnamese text analysis
#
# These are ESSENTIAL for our custom emotion ML model!
# ============================================================
